<div class="comments">
  {{#each this}}
  <section class="comment">
    <div class="text">
      {{comment_name}}
    </div>
    <div class="meta">
       Created by {{user.username}} on {{format_date created_at}}
    </div>
  </section>
  {{/each}}
</div>


