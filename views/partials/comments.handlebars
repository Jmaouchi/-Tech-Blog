<div class="comments">
  {{#each this}}
  <section class="comment">
    <div class="meta">
       Created by {{user.username}} on {{created_at}}
    </div>
    <div class="text">
      {{comment_name}}
    </div>
  </section>
  {{/each}}
</div>


